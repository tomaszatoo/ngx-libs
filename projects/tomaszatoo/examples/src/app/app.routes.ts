import { Routes } from '@angular/router';
import { NgxWpApiComponent } from './example/component/ngx-wp-api.component';

export const routes: Routes = [
    { path: 'ngx-wp-api', component: NgxWpApiComponent },

];
